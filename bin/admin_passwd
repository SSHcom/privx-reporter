#!/bin/bash
# Stable dev entrypoint. Implementation: apps/python/lib/_admin/passwd_run.

set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$(realpath "$script_dir/..")"

exec "$repo_root/apps/python/lib/_admin/passwd_run" "$@"
